# 🚀 НАЧНИТЕ ОТСЮДА!

## ГДЕ ВЫПОЛНЯТЬ КОМАНДЫ?

**НА ВАШЕМ КОМПЬЮТЕРЕ (macOS)**, а не на сервере!

---

## 📋 Пошаговая инструкция

### Шаг 1: Откройте Терминал на вашем Mac

Есть несколько способов:

**Способ 1: Через Spotlight**
- Нажмите `Cmd + Space`
- Введите: `Terminal`
- Нажмите `Enter`

**Способ 2: Через Finder**
- Откройте `Finder`
- Перейдите: `Программы` → `Утилиты` → `Terminal`

**Способ 3: Через Launchpad**
- Откройте `Launchpad`
- Найдите папку `Другие`
- Нажмите на `Terminal`

---

### Шаг 2: Перейдите в директорию проекта

Скопируйте и вставьте эту команду в терминал:

```bash
cd /Users/yernar/Oyna-Web-site
```

Нажмите `Enter`.

---

### Шаг 3: Запустите скрипт деплоя

Скопируйте и вставьте эту команду:

```bash
./deploy-to-server.sh
```

Нажмите `Enter`.

---

## 🎬 Что будет происходить?

После запуска скрипта:

1. **Скрипт подключится к серверу**
   - Вас попросят ввести пароль: `k4рТ?3K33%аї`
   - Введите пароль и нажмите `Enter` (пароль не будет виден при вводе - это нормально!)

2. **Скрипт выполнит настройку**
   - Установит PM2
   - Клонирует репозиторий
   - Установит зависимости
   - Соберет проект
   - Запустит приложение

3. **Скрипт попросит настроить SSL**
   - Следуйте инструкциям на экране
   - Откройте Plesk панель в браузере
   - Установите SSL сертификат

---

## 📝 Полный пример

Вот как это выглядит в терминале:

```bash
# Ваш Mac - Терминал
MacBook-Pro:~ yernar$ cd /Users/yernar/Oyna-Web-site
MacBook-Pro:Oyna-Web-site yernar$ ./deploy-to-server.sh

🚀 Деплой Oyna Website на oynapp.kz

📋 Шаг 1: Подключение к серверу и настройка
Подключаемся к oynapp.kz@195.210.46.29...

oynapp.kz@195.210.46.29's password: [введите пароль]
✅ Подключились к серверу
✅ Node.js: v20.11.1
📦 Устанавливаем PM2...
...
```

---

## ⚠️ ВАЖНО!

### Скрипт выполняется на ВАШЕМ компьютере, а не на сервере!

- ✅ **Правильно:** Открыть терминал на Mac → выполнить команды
- ❌ **Неправильно:** SSH на сервер → выполнить команды

Скрипт **сам подключится** к серверу и выполнит там все необходимые действия.

---

## 🔍 Альтернативный вариант (если скрипт не работает)

Если по какой-то причине скрипт не запускается, вы можете выполнить все вручную.

### Вариант А: Через терминал на Mac (подключение к серверу)

```bash
# 1. Подключитесь к серверу
ssh oynapp.kz@195.210.46.29
# Введите пароль: k4рТ?3K33%аї

# 2. На сервере выполните:
cd /var/www/vhosts/oynapp.kz/httpdocs
npm install -g pm2
pm2 startup
git clone https://github.com/midneuron/oyna-website.git .
npm install
npm run build
pm2 start npm --name "oynapp" -- start
pm2 save
pm2 status
```

### Вариант Б: Через Cursor (текущий редактор)

Если вы сейчас в Cursor:

1. Откройте встроенный терминал: `View` → `Terminal` или `Ctrl + ~`
2. В терминале Cursor выполните:
   ```bash
   ./deploy-to-server.sh
   ```

---

## 🆘 Проблемы?

### ❌ "Permission denied"

Выполните:
```bash
chmod +x deploy-to-server.sh
./deploy-to-server.sh
```

### ❌ "No such file or directory"

Проверьте, что вы в правильной директории:
```bash
pwd
# Должно вывести: /Users/yernar/Oyna-Web-site

ls deploy-to-server.sh
# Должно показать файл
```

### ❌ "Connection refused" или "Host key verification failed"

Попробуйте сначала подключиться вручную:
```bash
ssh oynapp.kz@195.210.46.29
# Введите пароль
# Если подключение работает, выйдите: exit
# Затем запустите скрипт снова
```

---

## 🎯 Итого:

1. **Откройте терминал** на вашем Mac
2. **Перейдите в проект:** `cd /Users/yernar/Oyna-Web-site`
3. **Запустите скрипт:** `./deploy-to-server.sh`
4. **Следуйте инструкциям** на экране

**Всё! 🚀**

---

## 📚 Дополнительная информация

После успешного выполнения скрипта:
- Сайт будет доступен на **https://oynapp.kz**
- PM2 будет управлять процессом
- При каждом `git push` будет автоматический деплой

Подробности в [DO_THESE_STEPS.md](./DO_THESE_STEPS.md)



---
alwaysApply: true
---
## 🎨 Фронтенд (Next.js):

- Используй *Next.js с TypeScript*.
- Используй *строго структуру pages/* для всех страниц. *Не создавай app/*, даже если проект поддерживает App Router.
- Стили оформляй через *Tailwind CSS* или *CSS-модули*.
- Все компоненты — *функциональные* и *переиспользуемые*, с соблюдением принципа SRP (одна ответственность).
- Все интерфейсы должны быть *адаптивными по умолчанию*: обязательно используй компактный mobile-first подход, применяя Tailwind breakpoints (sm, md, lg, xl) или @media-запросы.
- Формы реализуй с помощью react-hook-form или Formik.
- API-запросы делай через axios, SWR или React Query.
- Строго соблюдай структуру проекта:
  - components/
  - pages/
  - hooks/
  - store/
  - context/
  - styles/
  - lib/
  - utils/

---

## 📈 Оптимизация и SEO (Next.js):

- Используй *Server-Side Rendering (SSR)* или *Static Site Generation (SSG)* для публичных страниц.
- Обеспечь SEO-оптимизацию:
  - через next/head (title, meta, og:),
  - с помощью getServerSideProps / getStaticProps для загрузки данных,
  - с <Image /> из next/image — для автоматической оптимизации изображений и lazy loading.
- Для приватных маршрутов (личные кабинеты и т.п.) используй CSR с авторизацией.

---

## 📦 Производительность и UX (Next.js)

- Использовать SSR (getServerSideProps), SSG (getStaticProps) и ISR (revalidate) в зависимости от типа данных  
- При SSR настраивать кэширование вручную через заголовок Cache-Control  
- При SSG и ISR организовать долгосрочный кэш на уровне CDN или Nginx  
- Включать параметр revalidate в getStaticProps для гибкой генерации страниц  
- Кэшировать все статические ресурсы (/_next/static/, /images/, favicon.ico) на год (max-age=31536000, immutable)  
- Применять ленивую загрузку только для компонентов вне viewport, контент above-the-fold загружать сразу  
- Использовать next/dynamic с loading-fallback и ssr: false только при необходимости  
- Подключать изображения через <Image /> с priority={true} для ключевых блоков  
- Обеспечивать мгновенный рендер визуальных блоков и форм с предзагрузкой данных и использованием предсказуемого skeleton UI  
- Использовать CDN для доставки и кэширования всего контента  

---

## 🔁 Управление состоянием (State Management):

- Начинай с useState, useReducer, Context API.
- Для сложных сценариев используй *Redux Toolkit*:
  - @reduxjs/toolkit + react-redux
  - createSlice, configureStore, createAsyncThunk
  - Все слайсы и store — строго модульно, по папкам.
- Не используй Redux без необходимости — *в малых проектах предпочтительнее Context + Reducer*.

---

## 🔧 Бэкенд (отдельный, через Node.js + Express):

- Бэкенд должен быть отделён от Next.js и находиться в папке /backend.
- Используй *Node.js + Express* как серверную часть.
- ORM — только *Prisma*. Не используй raw SQL-запросы.
- Валидация данных — через zod или Joi.
- Все маршруты, middleware, контроллеры и схемы должны быть разделены модульно.
- Документируй API (Swagger или комментарии).
- Используй только *PostgreSQL* как базу данных (для авторизации, пользовательских данных и безопасности).

---

## 📊 Дополнительно:

- Таблицы: react-table, TanStack Table
- PDF: pdf-lib, react-pdf, html2pdf.js
- Графики: chart.js, recharts, apexcharts
- Код должен быть совместим с LMS, CMS и другими админ-панелями
- Если для определенной специфичной задачи в данном файле нет готового решения / подсказки по фреймворкам или библиотекам, то самостоятельно, используя доступ в интернет, найди лучшее решение

---

## 🧪 Dev Workflow

- Используй *Prettier + ESLint* для стилистики и чистоты кода.
- Пиши тесты:
  - Jest — для unit-тестов,
  - React Testing Library — для компонентов,
  - Supertest — для тестирования API.
- Все переменные окружения должны быть сохранены в .env, использоваться через process.env, и загружаться с помощью dotenv.
- *Никогда не захардкоживай пароли, API-ключи или секреты в коде.*
- Поддерживай чистую, масштабируемую архитектуру, пригодную для развития и командной работы.

---

## 🚀 CI/CD через GitHub Actions

- Проект должен быть готов к автоматической сборке и деплою с помощью *GitHub Actions*.
- В корне проекта должна быть создана папка *.github/workflows/*, в которой находятся .yml-файлы для автоматизации:
  - deploy.yml — автоматическая сборка и деплой при пуше в main или production;
  - lint.yml — проверка ESLint + Prettier;
  - test.yml — запуск юнит- и компонентных тестов.

### Пример структуры:


.github/
└── workflows/
    ├── deploy.yml
    ├── lint.yml
    └── test.yml


### Пример deploy.yml:

yaml
name: Deploy to Vercel

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Установка зависимостей
        run: npm install
      - name: Сборка проекта
        run: npm run build
      - name: Деплой (Vercel CLI)
        run: npx vercel --prod --token=${{ secrets.VERCEL_TOKEN }}


- Все токены, секреты (например, VERCEL_TOKEN, DATABASE_URL, JWT_SECRET) должны храниться в разделе *GitHub Repository → Settings → Secrets → Actions, и **никогда не попадать в репозиторий*.
- Все CI-скрипты должны быть кроссплатформенными, поддерживать *Linux (Ubuntu)*.
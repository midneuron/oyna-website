# 🚀 Настройка GitHub Actions и Vercel

## Быстрый старт

Следуйте этим шагам для настройки автоматического деплоя и CI/CD.

---

## Шаг 1: Установка зависимостей

```bash
npm install
```

Эта команда установит все необходимые зависимости, включая Jest, React Testing Library и Prettier.

---

## Шаг 2: Получение Vercel токенов

### 2.1 Создание токена Vercel

1. Откройте [Vercel Tokens](https://vercel.com/account/tokens)
2. Нажмите **"Create Token"**
3. Укажите название: `GitHub Actions Deploy`
4. Выберите Scope: **Full Account**
5. Скопируйте сгенерированный токен (он больше не будет показан!)
6. Сохраните его как `VERCEL_TOKEN`

### 2.2 Получение Project ID и Org ID

**Вариант 1: Через командную строку**

```bash
# Установите Vercel CLI, если еще не установлен
npm i -g vercel

# Залогиньтесь
vercel login

# Свяжите проект
vercel link

# После этого откройте файл .vercel/project.json
cat .vercel/project.json
```

Вы увидите:
```json
{
  "orgId": "team_xxxxxxxxxxxxx",
  "projectId": "prj_xxxxxxxxxxxxx"
}
```

**Вариант 2: Через Vercel Dashboard**

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings** → **General**
4. Найдите **Project ID** (скопируйте)
5. Откройте **Team Settings** → найдите **Team ID** (это ваш Org ID)

---

## Шаг 3: Настройка GitHub Secrets

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**
4. Добавьте следующие секреты:

### Обязательные секреты:

| Название | Значение | Описание |
|----------|----------|----------|
| `VERCEL_TOKEN` | ваш токен | Токен из шага 2.1 |
| `VERCEL_ORG_ID` | team_xxxxx | Org ID из шага 2.2 |
| `VERCEL_PROJECT_ID` | prj_xxxxx | Project ID из шага 2.2 |

### Опциональные секреты (для production):

| Название | Значение | Описание |
|----------|----------|----------|
| `NEXT_PUBLIC_SITE_URL` | https://yourdomain.com | URL вашего сайта |
| `SMTP_HOST` | smtp.gmail.com | SMTP сервер для email |
| `SMTP_PORT` | 587 | SMTP порт |
| `SMTP_USER` | your@email.com | SMTP пользователь |
| `SMTP_PASSWORD` | app-password | SMTP пароль (App Password для Gmail) |
| `CONTACT_INBOX` | contact@yourdomain.com | Email для получения заявок |
| `TELEGRAM_BOT_TOKEN` | bot-token | Токен Telegram бота |
| `TELEGRAM_CHAT_ID` | chat-id | ID чата для уведомлений |
| `CODECOV_TOKEN` | codecov-token | Токен Codecov (опционально) |

---

## Шаг 4: Настройка Gmail SMTP (опционально)

Если вы используете Gmail для отправки уведомлений:

1. Откройте [Google Account Security](https://myaccount.google.com/security)
2. Включите **2-Step Verification** (2FA)
3. Перейдите в [App Passwords](https://myaccount.google.com/apppasswords)
4. Создайте новый App Password:
   - App: **Mail**
   - Device: **Other (Custom name)** → введите "Oyna Website"
5. Скопируйте сгенерированный пароль (16 символов)
6. Используйте этот пароль как `SMTP_PASSWORD` в GitHub Secrets

**Параметры для Gmail:**
```
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-gmail@gmail.com
SMTP_PASSWORD=app-password-16-chars
```

---

## Шаг 5: Настройка Telegram Bot (опционально)

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду: `/newbot`
3. Следуйте инструкциям и создайте бота
4. Скопируйте **токен бота** → это ваш `TELEGRAM_BOT_TOKEN`

**Получение Chat ID:**

**Вариант 1: Через @userinfobot**
1. Найдите [@userinfobot](https://t.me/userinfobot)
2. Отправьте команду `/start`
3. Бот покажет ваш ID → это ваш `TELEGRAM_CHAT_ID`

**Вариант 2: Через API**
1. Отправьте любое сообщение вашему боту
2. Откройте в браузере:
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   ```
3. Найдите `"chat":{"id":123456789}` → это ваш Chat ID

---

## Шаг 6: Проверка настройки

### Проверка локально:

```bash
# Проверка линтера
npm run lint

# Проверка форматирования
npm run format:check

# Запуск тестов
npm run test:ci

# Сборка проекта
npm run build
```

Все команды должны выполниться без ошибок ✅

### Проверка на GitHub:

1. Закоммитьте изменения в ветку:
   ```bash
   git add .
   git commit -m "feat: setup CI/CD workflows"
   git push origin your-branch
   ```

2. Создайте Pull Request

3. Проверьте вкладку **Actions** в GitHub - должны запуститься:
   - ✅ Lint workflow
   - ✅ Test workflow

4. После merge в `main` запустится:
   - ✅ Deploy workflow → Production deploy

---

## Шаг 7: Первый деплой

### Автоматический деплой через GitHub:

1. Merge PR в ветку `main`
2. GitHub Actions автоматически запустит деплой
3. Проверьте вкладку **Actions** для отслеживания прогресса
4. После завершения сайт будет доступен на Vercel

### Ручной деплой (альтернатива):

```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой в production
vercel --prod
```

---

## Шаг 8: Мониторинг

### GitHub Actions

Откройте вкладку **Actions** в вашем репозитории:
- Зеленая галочка ✅ = все проверки прошли
- Красный крестик ❌ = есть ошибки (кликните для деталей)

### Vercel Dashboard

Откройте [Vercel Dashboard](https://vercel.com/dashboard):
- Все деплои с их статусами
- Логи билдов
- Аналитика и мониторинг

---

## Troubleshooting

### ❌ Error: "VERCEL_TOKEN is not set"

**Решение:**
- Проверьте, что вы добавили `VERCEL_TOKEN` в GitHub Secrets
- Убедитесь, что название написано точно так же (с большими буквами)

### ❌ Error: "Invalid token"

**Решение:**
- Создайте новый токен на Vercel
- Обновите `VERCEL_TOKEN` в GitHub Secrets

### ❌ Error: "Project not found"

**Решение:**
- Проверьте правильность `VERCEL_PROJECT_ID` и `VERCEL_ORG_ID`
- Убедитесь, что проект существует в Vercel Dashboard

### ❌ Tests failing

**Решение:**
1. Запустите тесты локально: `npm run test:ci`
2. Исправьте падающие тесты
3. Закоммитьте исправления

### ❌ Lint errors

**Решение:**
```bash
npm run lint:fix     # Автофикс
npm run format       # Форматирование
git add .
git commit -m "fix: lint errors"
git push
```

---

## 📚 Дополнительные ресурсы

- [Документация GitHub Actions](https://docs.github.com/en/actions)
- [Документация Vercel](https://vercel.com/docs)
- [CI_CD_SETUP.md](./CI_CD_SETUP.md) - детальная документация по CI/CD
- [.env.ci.example](./.env.ci.example) - пример переменных окружения

---

## ✅ Чеклист готовности

- [ ] Установлены зависимости (`npm install`)
- [ ] Получен `VERCEL_TOKEN`
- [ ] Получены `VERCEL_ORG_ID` и `VERCEL_PROJECT_ID`
- [ ] Все секреты добавлены в GitHub Secrets
- [ ] Локальные проверки проходят (lint, test, build)
- [ ] Создан тестовый PR для проверки workflows
- [ ] Первый деплой прошел успешно
- [ ] Email уведомления работают (если настроены)
- [ ] Telegram уведомления работают (если настроены)

---

**🎉 Готово! CI/CD настроен и работает!**

Теперь при каждом push/PR будут автоматически:
- ✅ Проверяться код
- ✅ Запускаться тесты
- ✅ Деплоиться на Vercel

Удачной разработки! 🚀


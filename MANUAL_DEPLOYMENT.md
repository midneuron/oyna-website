# 🚀 Ручной деплой на oynapp.kz

## Вариант 1: Автоматический скрипт (РЕКОМЕНДУЕТСЯ)

Просто выполните:

```bash
./deploy-to-server.sh
```

Скрипт автоматически:
- ✅ Подключится к серверу через SSH
- ✅ Установит PM2 и настроит автозапуск
- ✅ Клонирует репозиторий
- ✅ Установит зависимости
- ✅ Соберет проект
- ✅ Запустит приложение
- ✅ Подскажет, как настроить SSL

---

## Вариант 2: Ручные команды

### Шаг 3: Настройка сервера

#### 3.1 Подключитесь к серверу:

```bash
ssh oynapp.kz@195.210.46.29
# Пароль: k4рТ?3K33%аї
```

#### 3.2 Перейдите в директорию сайта:

```bash
cd /var/www/vhosts/oynapp.kz/httpdocs
```

#### 3.3 Установите PM2:

```bash
npm install -g pm2
pm2 startup
```

#### 3.4 Создайте структуру директорий:

```bash
mkdir -p temp backups logs
chmod 755 temp backups logs
```

#### 3.5 Клонируйте репозиторий:

```bash
# Замените URL на ваш репозиторий
git clone https://github.com/your-username/oyna-website.git .
```

#### 3.6 Установите зависимости:

```bash
npm install
```

#### 3.7 Создайте .env.production:

```bash
cat > .env.production << 'EOF'
NEXT_PUBLIC_SITE_URL=https://oynapp.kz
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
CONTACT_INBOX=info@oynapp.kz
TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_CHAT_ID=your-chat-id
EOF
```

**Важно:** Замените значения на реальные!

#### 3.8 Соберите проект:

```bash
npm run build
```

#### 3.9 Запустите приложение:

```bash
pm2 start npm --name "oynapp" -- start
pm2 save
```

#### 3.10 Проверьте статус:

```bash
pm2 status
pm2 logs oynapp
```

---

### Шаг 4: Настройка SSL

#### 4.1 Откройте Plesk панель:

Откройте в браузере: **https://195.210.46.29:8443**

Или: **https://srv-plesk01.ps.kz:8443**

#### 4.2 Войдите:

- **Логин:** oynapp.kz
- **Пароль:** k4рТ?3K33%аї

#### 4.3 Установите SSL сертификат:

1. Перейдите в **Websites & Domains**
2. Выберите **oynapp.kz**
3. Нажмите на **SSL/TLS Certificates**
4. Найдите раздел **Let's Encrypt**
5. Нажмите кнопку **Install** или **Get it free**
6. В форме выберите:
   - ✅ Secure the domain: `oynapp.kz`
   - ✅ Include www subdomain: `www.oynapp.kz`
   - ✅ Secure mail: если используете email
7. Нажмите **Get it free** или **Install**

#### 4.4 Настройте HTTPS редирект:

1. В Plesk перейдите: **Websites & Domains** → **oynapp.kz**
2. Нажмите **Hosting Settings**
3. Найдите раздел **Security**
4. Включите:
   - ✅ **Permanent SEO-safe 301 redirect from HTTP to HTTPS**
   - ✅ **HSTS (HTTP Strict Transport Security)**
5. Нажмите **OK**

---

### Шаг 5: Первый деплой через GitHub

На вашем компьютере:

```bash
cd /Users/yernar/Oyna-Web-site

# Закоммитить изменения
git add .
git commit -m "feat: setup Plesk deployment for oynapp.kz"
git push origin main
```

Проверьте деплой:
1. Откройте GitHub
2. Перейдите в **Actions**
3. Проверьте статус workflow "Deploy to Plesk"

---

## 📊 Проверка работы

### После всех шагов:

```bash
# Проверка DNS
nslookup oynapp.kz
# Должно вернуть: 195.210.46.29

# Проверка сайта
curl -I https://oynapp.kz

# Проверка на сервере
ssh oynapp.kz@195.210.46.29
pm2 status
pm2 logs oynapp
```

### Откройте в браузере:

- https://oynapp.kz
- https://www.oynapp.kz

Оба адреса должны работать! ✅

---

## 🔧 Полезные команды для управления

### На сервере:

```bash
# Подключение
ssh oynapp.kz@195.210.46.29

# Статус приложения
pm2 status

# Логи
pm2 logs oynapp

# Логи только ошибок
pm2 logs oynapp --err

# Перезапуск
pm2 restart oynapp

# Остановка
pm2 stop oynapp

# Удаление из PM2
pm2 delete oynapp

# Мониторинг
pm2 monit

# Обновление кода вручную
cd /var/www/vhosts/oynapp.kz/httpdocs
git pull
npm install
npm run build
pm2 restart oynapp
```

---

## 🆘 Troubleshooting

### ❌ SSH подключение не работает

```bash
# Проверьте доступность сервера
ping 195.210.46.29

# Проверьте SSH
telnet 195.210.46.29 22

# Если не работает - проверьте:
# 1. Правильность пароля
# 2. SSH включен в Plesk
# 3. Файрвол на сервере
```

### ❌ PM2 не устанавливается

```bash
# Проверьте версию Node.js
node --version

# Должна быть 18+ или 20+
# Если нет - установите через Plesk панель
```

### ❌ npm install падает с ошибкой

```bash
# Очистите кэш и попробуйте снова
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### ❌ Сборка (npm run build) падает

```bash
# Проверьте логи
npm run build 2>&1 | tee build.log

# Проверьте переменные окружения
cat .env.production

# Проверьте, что все файлы на месте
ls -la
```

### ❌ SSL сертификат не устанавливается

**Причины:**
1. DNS еще не распространился (подождите 1-2 часа)
2. Домен недоступен извне
3. Порты 80 и 443 заблокированы

**Решение:**
1. Проверьте DNS: `nslookup oynapp.kz`
2. Проверьте доступность: `curl -I http://oynapp.kz`
3. Подождите и попробуйте снова

---

## ✅ Чеклист

После выполнения всех шагов убедитесь:

- [ ] SSH подключение к серверу работает
- [ ] PM2 установлен и настроен
- [ ] Репозиторий клонирован в `/var/www/vhosts/oynapp.kz/httpdocs`
- [ ] Зависимости установлены (`node_modules` существует)
- [ ] `.env.production` создан и заполнен
- [ ] Проект собран (`.next` директория существует)
- [ ] Приложение запущено (`pm2 status` показывает "online")
- [ ] SSL сертификат установлен
- [ ] HTTPS редирект работает
- [ ] Сайт открывается по https://oynapp.kz
- [ ] www.oynapp.kz тоже работает

---

## 🎉 Готово!

После выполнения всех шагов:
- ✅ Сайт доступен на **https://oynapp.kz**
- ✅ PM2 управляет приложением
- ✅ SSL сертификат активен
- ✅ Автоматический деплой настроен

**Следующий шаг:** Тестирование и запуск! 🚀


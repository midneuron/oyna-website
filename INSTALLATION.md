# 📦 Инструкция по установке

## Шаг 1: Установка зависимостей

После того, как вы склонировали репозиторий, установите все необходимые зависимости:

```bash
npm install
```

Эта команда установит:
- **Основные зависимости** (Next.js, React, Tailwind CSS, и т.д.)
- **Зависимости для разработки:**
  - Jest и React Testing Library для тестирования
  - Prettier для форматирования кода
  - Дополнительные типы для TypeScript

## Шаг 2: Настройка переменных окружения

### Для локальной разработки:

1. Скопируйте файл с примером:
   ```bash
   cp env.example .env.local
   ```

2. Откройте `.env.local` и настройте переменные:

```env
# URL сайта (для SEO)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Email уведомления (опционально)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
CONTACT_INBOX=contact@yourdomain.com

# Telegram уведомления (опционально)
TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_CHAT_ID=your-chat-id
```

### Для CI/CD:

Для настройки GitHub Actions и автоматического деплоя см. [GITHUB_ACTIONS_SETUP.md](./GITHUB_ACTIONS_SETUP.md).

## Шаг 3: Проверка установки

Убедитесь, что все работает корректно:

```bash
# 1. Проверка линтера
npm run lint
# Должно пройти без ошибок ✅

# 2. Проверка форматирования
npm run format:check
# Должно пройти без ошибок ✅

# 3. Запуск тестов
npm run test:ci
# Все тесты должны пройти ✅

# 4. Сборка проекта
npm run build
# Сборка должна завершиться успешно ✅
```

Если все команды выполнились успешно - установка прошла корректно! 🎉

## Шаг 4: Запуск проекта

### Development mode (разработка):

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Production mode (локально):

```bash
npm run build
npm run start
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Шаг 5: Разработка

### Структура проекта

```
📁 Oyna-Web-site/
├── 📁 .github/
│   ├── 📁 workflows/          # GitHub Actions CI/CD
│   │   ├── lint.yml
│   │   ├── test.yml
│   │   └── deploy.yml
│   ├── 📁 ISSUE_TEMPLATE/     # Шаблоны для Issues
│   └── PULL_REQUEST_TEMPLATE.md
├── 📁 __tests__/              # Тесты
│   ├── 📁 components/
│   └── 📁 pages/
├── 📁 components/             # React компоненты
│   ├── 📁 common/
│   ├── 📁 layout/
│   └── 📁 sections/
├── 📁 pages/                  # Next.js страницы
│   ├── 📁 api/
│   ├── _app.tsx
│   ├── _document.tsx
│   ├── index.tsx
│   └── privacy.tsx
├── 📁 locales/                # Переводы RU/EN
├── 📁 public/                 # Статика (изображения)
├── 📁 styles/                 # Глобальные стили
├── 📁 utils/                  # Утилиты
├── 📁 lib/                    # Библиотеки
├── 📁 types/                  # TypeScript типы
├── jest.config.js             # Конфигурация Jest
├── jest.setup.js              # Setup для тестов
├── .prettierrc                # Конфигурация Prettier
└── package.json               # Зависимости
```

### Рабочий процесс

1. **Создайте новую ветку:**
   ```bash
   git checkout -b feature/my-feature
   ```

2. **Разрабатывайте:**
   - Пишите код
   - Пишите тесты
   - Следите за качеством

3. **Перед коммитом:**
   ```bash
   npm run lint:fix      # Автофикс линтера
   npm run format        # Форматирование
   npm run test          # Проверка тестов
   ```

4. **Коммит и push:**
   ```bash
   git add .
   git commit -m "feat: add my feature"
   git push origin feature/my-feature
   ```

5. **Создайте Pull Request** в GitHub

## Шаг 6: Настройка CI/CD (опционально)

Для автоматического деплоя на Vercel и запуска тестов при каждом PR:

📖 Следуйте инструкциям в [GITHUB_ACTIONS_SETUP.md](./GITHUB_ACTIONS_SETUP.md)

## Часто задаваемые вопросы

### ❓ npm не найден

**Решение:**

Используйте локальный Node.js:

```bash
source setup-dev.sh
```

Или вручную:

```bash
export PATH="/Users/yernar/Oyna-Web-site/.local/node-v20.11.1-darwin-arm64/bin:$PATH"
```

### ❓ Тесты падают при установке

**Решение:**

Сначала установите зависимости, затем запустите тесты:

```bash
npm install
npm run test:ci
```

### ❓ Ошибка при сборке

**Решение:**

1. Удалите `.next` и `node_modules`:
   ```bash
   rm -rf .next node_modules
   ```

2. Переустановите зависимости:
   ```bash
   npm install
   ```

3. Соберите проект:
   ```bash
   npm run build
   ```

### ❓ Prettier конфликтует с ESLint

**Решение:**

Используйте автофикс:

```bash
npm run lint:fix
npm run format
```

### ❓ Форма не отправляется

**Решение:**

Проверьте переменные окружения в `.env.local`:
- SMTP настройки для email
- Telegram настройки для уведомлений

Для локальной разработки можно закомментировать отправку или использовать тестовые значения.

## 📚 Дополнительные ресурсы

- [README.md](./README.md) - Основная документация
- [GITHUB_ACTIONS_SETUP.md](./GITHUB_ACTIONS_SETUP.md) - Настройка CI/CD
- [CI_CD_SETUP.md](./CI_CD_SETUP.md) - Детальная документация по CI/CD
- [CHANGELOG.md](./CHANGELOG.md) - История изменений

## 🎉 Готово!

Теперь вы готовы к разработке! Удачи! 🚀

**Следующие шаги:**
1. Запустите `npm run dev`
2. Откройте http://localhost:3000
3. Начните разработку!

Если у вас возникли проблемы, создайте [Issue](https://github.com/your-username/oyna-website/issues) в GitHub.


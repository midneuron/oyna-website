# 🔧 Решение проблемы SSH подключения

## ❌ Ошибка: "Operation timed out"

Это означает, что SSH порт 22 недоступен. У нас есть несколько решений.

---

## 🔍 Диагностика

### Шаг 1: Проверка доступности сервера

Выполните в терминале:

```bash
# Проверка пинга
ping -c 4 195.210.46.29

# Проверка доступности порта 22
nc -zv 195.210.46.29 22

# Проверка альтернативных SSH портов
nc -zv 195.210.46.29 22222
nc -zv 195.210.46.29 2222
```

---

## ✅ Решение 1: SSH на нестандартном порту

Plesk часто использует **нестандартный порт** для SSH (не 22).

### Попробуйте эти порты:

```bash
# Порт 22222 (часто используется в Plesk)
ssh -p 22222 oynapp.kz@195.210.46.29

# Порт 2222
ssh -p 2222 oynapp.kz@195.210.46.29

# Порт 2200
ssh -p 2200 oynapp.kz@195.210.46.29
```

### Если один из портов работает:

Обновите скрипт деплоя:

```bash
# Отредактируйте deploy-to-server.sh
nano deploy-to-server.sh

# Найдите строки с ssh и scp и добавьте -p ПОРТ
# Например:
ssh -p 22222 oynapp.kz@195.210.46.29
scp -P 22222 /tmp/oyna-setup.sh oynapp.kz@195.210.46.29:/tmp/
```

---

## ✅ Решение 2: Включить SSH в Plesk

SSH может быть отключен. Давайте включим его через Plesk панель.

### Через веб-интерфейс Plesk:

1. Откройте: **https://195.210.46.29:8443**
2. Войдите с логином: `oynapp.kz`
3. Перейдите в: **Tools & Settings** (Инструменты и настройки)
4. Найдите: **SSH Terminal**
5. Включите: **Allow access to the server over SSH**
6. Сохраните настройки

**Альтернативный путь:**
1. **Websites & Domains** → **oynapp.kz**
2. **Web Hosting Access**
3. Включите: **Access to the server over SSH**
4. Выберите: **/bin/bash** как shell
5. Сохраните

---

## ✅ Решение 3: Использовать веб-терминал Plesk

Если SSH недоступен, можно использовать встроенный терминал Plesk!

### Шаги:

1. Откройте: **https://195.210.46.29:8443**
2. Войдите с логином: `oynapp.kz`
3. Найдите в меню: **SSH Terminal** или **Web Terminal**
4. Откроется терминал прямо в браузере!

### В веб-терминале выполните:

```bash
cd /var/www/vhosts/oynapp.kz/httpdocs

# Проверка Node.js
node --version

# Установка PM2
npm install -g pm2
pm2 startup

# Создание директорий
mkdir -p temp backups logs
chmod 755 temp backups logs

# Клонирование репозитория
git clone https://github.com/midneuron/oyna-website.git .

# Установка зависимостей
npm install

# Создание .env.production
cat > .env.production << 'EOF'
NEXT_PUBLIC_SITE_URL=https://oynapp.kz
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
CONTACT_INBOX=info@oynapp.kz
TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_CHAT_ID=your-chat-id
EOF

# Редактирование .env.production (замените значения на реальные)
nano .env.production

# Сборка проекта
npm run build

# Запуск приложения
pm2 start npm --name "oynapp" -- start
pm2 save

# Проверка статуса
pm2 status
pm2 logs oynapp
```

---

## ✅ Решение 4: Обратиться в поддержку PS.KZ

Если ничего не помогло, свяжитесь с хостером:

### Контакты PS.KZ:

- **Сайт:** https://ps.kz/
- **Поддержка:** support@ps.kz
- **Телефон:** указан на сайте

### Что спросить:

1. **"Какой порт используется для SSH?"**
2. **"Как включить SSH доступ для домена oynapp.kz?"**
3. **"Можете ли вы включить SSH для моего аккаунта?"**

---

## ✅ Решение 5: Использовать FTP для загрузки файлов

Временное решение - можно использовать FTP для загрузки файлов.

### Через FileZilla или любой FTP клиент:

```
Host: 195.210.46.29 (или ftp.oynapp.kz)
Username: oynapp.kz
Password: k4рТ?3K33%аї
Port: 21 (или 22 для SFTP)
```

### Что загрузить:

1. Соберите проект локально: `npm run build`
2. Загрузите через FTP:
   - Папку `.next`
   - Папку `public`
   - Файлы `package.json`, `package-lock.json`, `next.config.ts`

3. Через Plesk веб-терминал:
   ```bash
   cd /var/www/vhosts/oynapp.kz/httpdocs
   npm install --production
   pm2 start npm --name "oynapp" -- start
   ```

---

## 🔍 Дополнительная диагностика

### Проверка какие порты открыты:

```bash
# На вашем Mac
nmap 195.210.46.29

# Или онлайн
# Откройте: https://www.yougetsignal.com/tools/open-ports/
# Введите: 195.210.46.29
```

### Проверка через telnet:

```bash
# Проверка разных портов
telnet 195.210.46.29 22
telnet 195.210.46.29 22222
telnet 195.210.46.29 2222
```

---

## 📝 Обновленный скрипт для нестандартного порта

Если вы нашли рабочий порт, создайте новый скрипт:

```bash
#!/bin/bash
# deploy-with-custom-port.sh

SSH_PORT=22222  # Замените на ваш порт
SERVER_HOST="195.210.46.29"
SERVER_USER="oynapp.kz"

echo "Подключение через порт $SSH_PORT..."

ssh -p $SSH_PORT $SERVER_USER@$SERVER_HOST << 'ENDSSH'
cd /var/www/vhosts/oynapp.kz/httpdocs
git pull
npm install
npm run build
pm2 restart oynapp || pm2 start npm --name "oynapp" -- start
pm2 save
pm2 status
ENDSSH

echo "Деплой завершен!"
```

---

## 🎯 Рекомендованный порядок действий:

1. **Сначала попробуйте:** Веб-терминал в Plesk (Решение 3) - это самый быстрый способ!
2. **Затем:** Включите SSH в настройках Plesk (Решение 2)
3. **Попробуйте:** Нестандартные порты (Решение 1)
4. **Если не помогло:** Обратитесь в поддержку (Решение 4)

---

## ✅ Следующие шаги после решения SSH

После того, как подключение заработает:

1. Выполните команды из **MANUAL_DEPLOYMENT.md**
2. Настройте SSL через Plesk панель
3. Проверьте работу сайта на https://oynapp.kz

---

**Удачи! Если нужна помощь - пишите! 🚀**


